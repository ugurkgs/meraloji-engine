# Devir Notu — 30 Ağustos 2026

Bu dosya kendi başına yetmez. Sırayla **`DEVIR.md` (12 Ağu) → `DEVIR-17-AGUSTOS.md` → bu dosya** okunmalı; oradaki kurallar ve tuzaklar aynen geçerli, burada yalnız üstüne eklenenler var.

---

## 0 · Bir dakikada durum

| | |
|---|---|
| **Yayındaki APK** | **4.5.0 · versionCode 47**, 27 Ağustos'ta Play'de |
| **Yayındaki sunucu** | commit `c126ae0` (29 Ağu 19:39 TR) — deploy sonrası 12 nokta × 4 dil taraması 12/12 temiz |
| **species.js** | **877 kayıt** (875 → +ceran +mavraki → −kastroz) |
| **Kullanıcı hacmi** | 29 Ağu logu: **300 analiz, 18 kayıtlı kullanıcı**. Küçük ama gerçek. |
| **Bekleyen APK işleri** | 4 düzeltme yerelde derlenmiş, yayında DEĞİL (aşağıda §5) |

**İlk iş:** Crashlytics'te `4.5.0 (47)` kovasına bak. Yayın tarihinden ÖNCEKİ kayıtlar gerçek kullanıcı değil, geliştirme sırasında aynı numarayla kurulan test derlemeleridir.

---

## 1 · Uygulama nedir

**Meraloji** — Türkiye kıyıları için balık avı tahmini yapan Android uygulaması. Sahibi tek kişi geliştiriyor (Uğur Koğuş).

- **İstemci:** `meraloji-twa-package/meraloji-twa` — Java, saf LF satır sonu, git yalnız YEDEK
- **Sunucu:** `meraloji-engine` — Node/Express, ~12.000 satır `server.js`, **CRLF**, Render'da. **`git push origin main` = CANLI DEPLOY**
- **Tür veritabanı:** `species.js` — 877 tür, ~1,5 MB, `SPECIES_DB` dışa aktarımı
- **Nehir ağzı modeli:** `rivermouth.js` — 53 isimli + 113 küçük ağız, elle pinlenmiş

Kullanıcı haritada bir nokta seçer, sunucu o nokta için hava/deniz/uydu verisini toplar, 877 tür arasından uygun olanları puanlar ve bir "gösterge skoru" üretir. PRO aboneliği var; **Av Modu** (6 saatlik izleme + bildirim) PRO'ya özel.

---

## 2 · DOKUNMA KURALLARI — sahibin kalıcı istekleri

Bunlar önceki devir notlarından geliyor ve **hâlâ geçerli**:

1. **Git'i sahip kullanmıyor.** Push'u sen yapacaksın. Ama **her değişiklikten önce söyle, sonra yap.**
2. **Yereldeki `meraloji-twa-package/.../server.js` ÖLÜ KOPYA.** Güncel olan `meraloji-engine/server.js`. Yanlış dosyayı düzenleme.
3. **PRO kullanıcılar zarar görmesin, kayıp yaşamasın.** Mevcut kullanıcıyı etkileyen her şey ÖNCE onay ister.
4. **Metinler hardcode edilmez.** Dört dil: `tr`, `en`, `es`, `el`. Kodlamaya dikkat.
5. `local.properties`, `*.keystore`, `google-services.json` **asla git'e girmez**.
6. **Sunucu değişikliği APK'yı 4 kez çökertti.** Doğru sıra: **ÖNCE istemciyi yayınla, SONRA sunucudan yeni alan gönder.** İz olmadan tahmin yürütme.
7. **APK yayınlamadan ÖNCE** `tools/` altındaki denetimleri koştur: `null-guvenligi.py`, `layout-id-denetim.py`, `format-denetim.py`, `dil-denetim.py`, `kontrol-av-metinleri.js`, `kontrol-satin-alma-yolu.py`.

### 2.1 · Bu oturumda eklenen kurallar

**Paragat kapsam dışı.** Sahibin sözü: *"paragat avı benim uygulamam için uygun değil, bununla ilgili detayları yoksay. benim fikrim hobi balıkçılığına girmiyor."*
Kapsam İÇİ: olta (`O`), sırtı (`S`), parangula (`Pr`), zıpkın (`Z`), kürek (`K`).
Kapsam DIŞI: paragat (`P`), uzatma ağı (`UA`), gırgır (`G`), trol (`T`), pinter (`Pn`), dalyan (`D`).

**Riski abartma.** Sahip 30 Ağustos'ta şunu söyledi: *"bu konu için beni epey korkuttuğun için bu işe el atmak beni korkutuyor."* Bir iş acil değilse **"ortada yangın yok"u açıkça söyle.** Önce hiçbir şey değiştirmeyen bir ölçüm öner, kararı ondan sonra sun.

**Simülasyonun WAVE_HEIGHT'a dönmesi BİLİNÇLİ.** `MainActivity.updateUI` içinde her çağrıda mod sıfırlanıyor. Bunu hata sanıp düzeltmeye kalkma — sahip teyit etti, performans önlemi (diğer modlar harita snapshot'ı tetikleyip HUD açılışını takıyor).

---

## 3 · TUZAKLAR — hepsine bu oturumda bizzat düştüm

### 3.1 · ⚠ Python metin modu CRLF'i yok eder — ÜÇ KEZ düştüm

`server.js` ve `species.js` **CRLF**. Python'da:

```python
io.open(p, encoding='utf-8').read()      # ← \r\n'i \n'e ÇEVİRİR (universal newlines)
io.open(p, 'rb').read().decode('utf-8')  # ← DOĞRU, \r\n korunur
```

Metin modunda okuyup `m.find('\r\n    },')` ile blok sonu ararsan **hiç bulamazsın**, `-1` döner, `m[bas:-1]` dosyanın geri kalanı olur ve **sonraki kayıtların verisi karışır.**

Bu bana bir kez şunu söyletti: *"ceran ve mavraki'de de monthlyActivity var"* — ikisinde de yoktu. Doğru yöntem: **modülü `require` edip gerçek JS nesnesine bak**, metin ayrıştırma yapma.

### 3.2 · ⚠ `monthlyActivity`, `seasons`'ı tamamen eziyor

`server.js` (mevsim bloğu):

```js
if (fish.monthlyActivity && fish.monthlyActivity.length === 12) {
    seasonalEff = fish.monthlyActivity[monthToUse];   // seasons HİÇ OKUNMAZ
```

**14 tür bu durumda:** `lufer, istavrit, uskumru, kolyoz, hamsi, yazili_orkinos, palamut, cinekop, sardalya, lahoz, aterin, papalina, caca, tirsi`

Bu türlerde `seasons` düzeltmek **hiçbir şey yapmaz**. Bir türün mevsimselliğine dokunmadan önce `monthlyActivity` var mı diye BAK.

*(Bu yüzden `uskumru` düzeltmesi yarım kaldı — §6'ya bak.)*

### 3.3 · ⚠ Taksonomi: kitap eski adı, species.js güncel adı kullanıyor

Eksik tür listesi çıkarırken latince eşleştirme yaparsan **eşanlamlıları kaçırırsın**:

| kaynak (2017) | species.js |
|---|---|
| *Liza aurata* | *Chelon auratus* |
| *Scomber japonicus* | *Scomber colias* |
| *Sciena umbra* | *Sciaena umbra* |

İlk taramamda "46 tür eksik" dedim; epitetle yeniden tarayınca gerçek sayı çok daha azdı. **Cins düzeyinde yedek eşleştirme de sahte çift üretir** (Kolyoz → uskumru gibi) — ayrı raporla.

### 3.4 · ⚠ Anahtar adları yanıltıcı ama VERİ doğru

`species.js`'de anahtarlar eskimiş:

| anahtar | taşıdığı tür | görünen ad |
|---|---|---|
| `fangri` | *Pagellus erythrinus* | "Alyanak (Kırma Mercan)" ✓ |
| `zurna` | *Sphyraena sphyraena* | "İskarmoz" ✓ |
| `akya` | *Seriola dumerili* | "Akya (Sarıkuyruk)" ✓ |

Bunları "ad hatası" sanıp düzeltmeye kalkma — **kullanıcıya giden veri doğru**, yalnız anahtarlar eski. Ben iddia ettim, yanlış çıktı.

### 3.5 · ⚠ Ölçüm aracının kendisi bozuk olabilir

- Akran bandını `category: LAGUN` üzerinden kurdum → n=3 çıktı ve üçünün de `tempRange.min` değeri tam 10'du. p10=p90=10 olunca **10'dan farklı her değer otomatik "sapma"** işaretlendi. Dejenere dağılım, sinyal değil. Doğru akran kümesi **familya** (Mugilidae, n=5).
- Takvim doğrulamasında beraberlikleri "çelişki" saydım (`.index(max())` ilk indisi döndürür). Lüfer deseninde kış ve sonbahar ikisi de 3/3'tü.
- Bir regresyon sınamam **boşa geçti**: mekanizmasız tür dalını `kikla` ile sınadım, o `YEM_HIRSIZI`'nda olmadığı için hiç uyarı üretilmiyordu, iddia bedavaya doğru çıkıyordu.

**Kural:** bir oran şaşırtıcı çıkarsa (bende 21 satırın 17'si "çelişki" çıktı) **önce ölçüm aracından şüphelen.**

### 3.6 · ⚠ Belgeyi koda eşitlemeden ÖNCE hangisinin yeni olduğuna bak

`OZEL-UYARI-METINLERI.md` ile `server.js` arasında 12 maddenin 8'inde sapma buldum ve **belgeyi koda eşitledim.** Yanlış yöndü:

```
.md         26 Ağustos   "metinler sahibin eliyle"
server.js   25 Ağustos
```

Belge koddan **yeniydi** — sahibin yazdığı metinler koda hiç geçirilmemişti. Bir günlük eskiyle ezdim, `git checkout` ile geri aldım. **Önce `git log --format="%h %ad %s" -- dosya` çalıştır.**

### 3.7 · ⚠ Android `res/` altına yedek dosya koyma

`strings.xml.yedek-...` dosyalarını `res/values/` içine koydum, `mergeDebugResources` patladı:

> `The file name must end with .xml`

Yedekleri **res ağacının dışına** al.

### 3.8 · Küçük ama zaman yiyen tuzaklar

- **PowerShell içinde `python -c "..."`** — kaçış karakterleri sürekli patlıyor. Betiği **dosyaya yaz**, sonra çalıştır.
- **Yol içindeki `(12)`** cmd'yi bozuyor. `gradlew.bat` için: `& "$P\gradlew.bat" -p "$P" ...`
- **Bash aracı** bu makinede `/c/Users/...` yollarını göremiyor; PowerShell kullan.
- **Karakter ≠ bayt.** `len(m)` (çözülmüş metin) ile `len(b)` (bayt) karşılaştırıp "dosya 53 KB büyüdü" diye paniğe kapıldım. Türkçe/Yunanca harfler UTF-8'de çok baytlı.
- **`X-App-Version` biçimi `"47/4.5.0"`** — önce versionCode. `"4.5.0 (47)"` gönderirsen `4` olarak ayrıştırılır, "eski istemci" sayılırsın ve `ozelUyarilar` alanı yanıttan silinir.
- **`toLocalHour(d, utcOffsetSeconds)` SANİYE alır.** 3 geçersen 3 saniye ekler.
- **Anonim yanıt sanitize edilir:** `hourlyScores`, `activityWindows`, `bestHourScore` gelmez. PRO davranışını anonim istekle ölçemezsin.

---

## 4 · Bu oturumda canlıya giden — commit `c126ae0`

### 4.1 · species.js (875 → 877)

| iş | ayrıntı |
|---|---|
| **+`ceran`** (*Chelon ramada*) | İzmir av takviminde oltayla avlanan, Türkiye bölgesiyle karşılığı olmayan tür |
| **+`mavraki`** (*Chelon labrosus*) | species.js'de vardı ama `uk_grey_mullet` anahtarıyla, `regions:["UNITED_KINGDOM"]` kilitli. O kayda **dokunulmadı**, ayrı kayıt açıldı |
| **−`kastroz`** | Eklendi, sonra **sahip kararıyla silindi**. Yeniden ekleme. Kod tabanının tamamı tarandı, hiçbir kod atıfı yok |
| `eskina` sezonları | yaz 0,80 → 0,35; sonbahar 0,40 → **0,90**. Eski yaz zirvesi tam olarak YUMURTLAMA dönemiydi |
| `sinarit` sezonları | sonbahar 0,65 → **0,90**, ilkbahar 0,70 → 0,50 |
| `sarikulak` sezonları | sonbahar 0,60 → **0,85**. Toplam bilerek sabit tutuldu (2,55 → 2,55): saf dönüş |
| `uskumru` sezonları | ilkbahar 0,85 → 0,40, sonbahar → 0,90. **AMA ÖLÜ** — §3.2 |

### 4.2 · server.js — özel uyarı tavsiye metinleri (5 madde × 4 dil)

Hepsi **48 karakterin altında** (ölçülen tek satır bütçesi) ve **nokta ile bitmiyor** (sahibin isteği):

```
kucukAgiz  Yemi sağlam bağlayın veya büyük iğne deneyin        (44)
sertGaga   Sert gagalı türlerde çelik köstek deneyin           (41)
gagalar    Yem hırsızı: Yemi iple sıkıca bağlayın              (38)
sertYem    Sert yem kullanın: madya, kalamar veya uskumru      (46)
bolYem     Yeminiz tükenebilir, yanınıza ekstra yem alın       (45)
```

**Karakter bütçesi nereden geldi** (`MainActivity.ozelUyarilariCiz` ölçüldü, 360dp telefon):

| alan | yazı boyu | bütçe |
|---|---|---|
| Başlık | 14,5sp (PRO rozetiyle aynı satır) | **28** |
| Gövde | 12sp | **96** (2 satır) |
| Tavsiye | 11,5sp + madde işareti | **48** (1 satır) |

Kodda `setMaxLines`/`setEllipsize` **YOK** — taşan metin kesilmez, sarar. Bütçe aşımı veri kaybı değil, kart uzaması.

---

## 5 · BEKLEYEN APK İŞLERİ — yerelde derlenmiş, YAYINDA DEĞİL

Bir sonraki sürümle çıkacak dört düzeltme:

| iş | commit / durum |
|---|---|
| **Hayalet uyarı rozeti** | `82a7cb5` — `ozelUyarilariCiz` uyarısız analizde erken dönüyor, rozet önceki analizden kalıyordu. Yalnız 4.5.0'ı etkiliyor, kozmetik |
| **Favori skorları anonim yoldan gidiyordu** | `c175b31` — `analyzeAnon` yerine kimlik doğrulamalı çağrı. PRO kullanıcı anonim IP kotasını yiyordu |
| **Sessiz tazeleme** | `updateUI` 4 parametreli oldu (`kartiYenidenAc`). Arka planda uydu SST'si gelince kart artık kendini kapatıp açmıyor, kaydırma korunuyor |
| **Dakika hassasiyetli olaylar** | `AvOlayUretici` — §7 |

Hepsi `compileDebugJavaWithJavac` ile derlendi, `tools/` denetimlerinden geçti.

---

## 6 · AÇIK İŞLER — ölçüldü, uygulanmadı

### 6.1 · `uskumru` `monthlyActivity` — yarım kalan iş

Sahibin onayladığı sezon düzeltmesi §3.2 yüzünden **skora yansımıyor**. Mevcut dizi (Oca→Ara):

```
0.55 0.5 0.7 0.85 0.9 0.4 0.3 0.35 0.6 0.8 0.75 0.6
```

Mayıs 0,9 ile zirve — kaynaklar ise **Eylül-Şubat** diyor. Kayıt şu an kendi içinde çelişkili: `seasons` sonbahar, dizi ilkbahar. Zararsız ama eksik.

### 6.2 · Gösterge skoru yem hırsızlarından besleniyor

`calcAvgScore` yalnız `İSTİLACI / KORUMA / TİCARİ` eliyor. Sonuç:

```
Bostanlı %70  ←  Horozbina 72, Sarpa 68, Lokum 64   (üçü de yem hırsızı)
Urla     %70  ←  Trakun 70, Lokum 70, İskarmoz 70
```

Kullanıcı "skorlar yüksek geliyor" dedi; sebebi bu. `AV_DEGERI` skoru **etkilemiyor**, yalnız etiket üretiyor. Düzeltme ayrı bir karar.

### 6.3 · ⚠ Yanıt önbelleği `lang` ile anahtarlanmıyor

```js
const cacheKey = `forecast_v24_${gLat}_${gLon}_h${clickHour}`;   // TTL 3 saat
```

**Dil yok.** Bir koordinatı ilk kim hangi dille sorarsa, sonraki 3 saat aynı hücreyi soran herkes o dilde yanıt alıyor. Ölçtüm: **3/3 kirlendi**, kontrol grubunda 2/2 temiz. Satır **9 Mart 2026** tarihli (`e95e388`), yeni bir hata değil.

### 6.4 · Diğer

- `lahoz`, `yilan_baligi`, `goliath_grouper` korunan/kısıtlı olduğu hâlde `advice.bait` taşıyor (orfoz doğru yapılmış)
- `baitEl` alanı 875 türün 869'unda İngilizce
- `ind_grey_mullet` sezonları neredeyse düz (0,85/0,90/0,90/0,90) — familya bandını tek başına şişiriyor
- `zargana` sezonları şüpheli ama **tek kaynak** var, yetersiz — dokunma
- **anonFree tavanı** IP başına günlük 30. 29 Ağu'da 8 kez tetiklendi. Firebase Anonymous Auth'a bağlı, **acil değil**

---

## 7 · Av Modu — dakika hassasiyeti (son iş)

### Ne bulundu

`AvOlayUretici.yukariKesim`, eşiğin `a < eşik && b >= eşik` olduğu `i` indisini döndürüyordu. Geçiş `(i-1, i]` aralığında ama kod **aralığın sonunu** raporluyordu → olaylar **sistematik 0-60 dk GEÇ**, ortalama 30.

### Ne yapıldı

- `yukariKesim` / `asagiKesim` artık **kesirli saat** döndürüyor (doğrusal ara değer)
- Basınç düşüşü ayrı diziye çevrildi, o da dakikalı
- `an(double)` — kesirli saat → gerçek zaman, **10 dakikaya yuvarlar**
- `avo_pencere_m2` (4 dil) — pencere artık **aralık** gösteriyor

### ⚠ Ne YAPILAMADI ve neden

Sahip "17:40–18:20" gibi dar bir ısırma penceresi istedi. **Tür skor eğrisinden dürüstçe türetilemiyor.** 8 noktada ölçtüm:

```
Urla: 54 54 54 54 59 59 66 66 66 54 54 54 54 54 66 66 66 66 59 ...
```

Zirve sivri uç değil, **3-4 saatlik plato**, üstelik aynı değer gün içinde tekrar ediyor.

| eşik | ortanca pencere |
|---|---|
| zirvenin %90'ı | 295 dk |
| %95 | 195 dk |
| **%98** | **142 dk** |

Dakika hassasiyeti **güneş açısından** gelir, skordan değil: `activityWindows` sunucuda SunCalc ile üretiliyor ve gerçek dakika taşıyor (İzmir bugün 02:40 / 14:47 / 18:17). İstemci `end` alanını hiç okumuyordu; artık okuyor.

**Zirve olayına bilerek dokunulmadı** — plato ölçümü orada dakika iddia etmeyi haksız kılar.

### Alanların ara değere uygunluğu (6 nokta, canlı)

| alan | ardışık fark sıfır | |
|---|---|---|
| score | %25 | en sürekli |
| wind | %32 | sürekli |
| wave / clarity | %41-42 | sınırda |
| current / pressure / temp | %68-78 | basamaklı |
| rain | %90 | çok basamaklı |

---

## 8 · Ege Ü. "İzmir Balıkçılığı" kaynağı

304 sayfalık akademik kaynak okundu ve ayıklandı → **`IZMIR-BALIKCILIGI-KAYNAK-NOTU.md`** (aynı klasörde, git'e girmedi).

**İçinde ne var:** 48 tür × 12 ay av takvimi (piksel ölçümüyle çıkarıldı — gri gölge metin katmanında yok), 9 ticari canlı yem, zehirli tür künyeleri + ilk yardım, dalyan/lagün göç tetikleyicileri, koordinatlı yapay resifler, körfez biyokütle haritası.

**Isınma sorusu ölçüldü:** kaynak 2008-2016 dönemine dayanıyor. NOAA OISST ile 2017-2025 karşılaştırıldı → yıllık **+0,39…+0,50 °C**, faz kayması **4-6 gün**. Takvimi geçersiz kılmıyor.

⚠ **Takvim `seasons`'a doğrudan kopyalanmaz.** Gırgır/trol yasağı 15 Nisan – 1 Eylül; `G`/`T` satırları balığı değil **mevzuatı** ölçüyor. Hamsi'nin `XXX.....XXXX` deseni bunun kanıtı.

---

## 9 · Ölçüm disiplini — sahibin çalışma tarzı

Bu sahip **ölçüme dayalı** çalışıyor ve bunu bekliyor:

- Bir sayı sunmadan önce **ölç**. "Muhtemelen" ile karar verme.
- Gerçek kodu çıkarıp `vm` kutusunda koştur (`server.js`'i `require` etmek sunucuyu ayağa kaldırır).
- **Her sınamaya pozitif kontrol koy.** Yalnız yeniyi sınamak, eskiyi bozduğunu göremez.
- Yeni/değişen tür kaydını **akran bandıyla** karşılaştır (`CLAUDE-KONSOL-TALIMATI.md` §5, "Trakonya vakası").
- Bir oturumda bir madde.
- Hata yaparsan **düzelt ve söyle**; sahip yanlış bilgiyi sessizce taşımaktansa düzeltmeyi tercih ediyor.
- `node --check` sözleşmeyi doğrulamaz. Dönüş tipi değişikliği bir kez yayına gitti (`instant.temp` nesne döndü) — **uçtan uca yanıtı gör.**

---

## 10 · Sıradaki iş için öneri

Sahip Av Modu'na sunucu+istemci yenilikleri istedi. Sıralamam:

1. **Oturum sonu "tuttun mu?"** → `catchReports` koleksiyonunu besler. Şema zaten bunun için tasarlanmış: *"Skor saklarsak gözlem o motora çakılı kalır. Koşul saklarsak, motoru değiştirdiğimizde eski gözlemleri yeniden puanlayabiliriz."* Zamanla kullanıcı kendi isabetini görür.
2. **Mevsim geçidi bildirimi** — kitaptaki furya bulgusu (Eylül sonu + su soğuması + karanlık gece + lagün ağzı). Üç bileşen sunucuda, dördüncüsü `rivermouth.js`'te. Kimsede yok.
3. Oturum ortası koşul değişimi · 4. Geçmişten öğrenme · 5. Komşu sinyali

⚠ 3-5 **sunucunun oturum konumunu bilmesini** gerektiriyor. Şu an `POST /api/av-modu` yalnız "biri açtı/kapattı" sayıyor, konum göndermiyor — bu bilinçli (pil + gizlilik). Ama oturum tazelemeleri zaten `/api/forecast`'e lat/lon ile gidiyor; tek bir `&av=1` parametresi yeni pil maliyeti olmadan kilidi açar.

**18 kullanıcıyla sosyal özellikler (5) erken.** Kendi isabetini görmek (1) tek kullanıcıyla bile anlamlı.

---

## 11 · Yedekler

Bu oturumda alınan yedekler (aynı klasörde, git'e girmedi):

```
species.js.yedek-20260829-174901   (kefal eklemeleri öncesi)
species.js.yedek-20260829-183856   (sezon düzeltmeleri öncesi)
species.js.yedek-20260829-185016   (kastroz silme öncesi)
server.js.yedek-20260829-190029    (ilk sertYem denemesi öncesi)
server.js.yedek-20260829-191618    (son tavsiye metinleri öncesi)
```

İstemci tarafı: `MainActivity.java.yedek-20260830-011114`, `AvOlayUretici.java.yedek-20260830-020433`

⚠ Yedekleri **`res/` ağacının içine koyma** (§3.7).
